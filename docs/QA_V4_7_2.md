# QA v4.7.2

Offline checks included in the bundle:

- Python syntax compile for v4.7, v4.7.1, v4.7.2 builder/validator.
- v4.7 self-tests PASS.
- v4.7.1 self-tests PASS.
- v4.7.2 builder self-test PASS.
- v4.7.2 validator self-test PASS.
- Top20 exact SKU mapping priority tested.
- Multi-expiry stock total tested.
- Both sales/stock comparison ratios tested.
- Expiry-alert-only filtering tested without an invented threshold.

Production DB build is intentionally not claimed until the package is executed against the user's PostgreSQL data and the validator passes.
